# Быстрый старт - Система каталогов франшизы

## 🚀 За 5 минут до запуска

### 1. Установите сертификат

```bash
# Скопируйте сертификат в нужную директорию
cp terehin_n.cloud.mda-medusa.ru.p12 /root/5lb/backend/src/certs/

# Установите права доступа
chmod 600 /root/5lb/backend/src/certs/terehin_n.cloud.mda-medusa.ru.p12
```

### 2. Настройте переменные окружения

Добавьте в `/root/5lb/backend/.env`:

```env
# Balance API для франшизы
BALANCE_API_URL=https://cloud.mda-medusa.ru/fransh-trade/hs/Api/BalanceData
BALANCE_API_USERNAME=ТерехинНА
BALANCE_API_PASSWORD=123455123
CERT_PATH=src/certs/terehin_n.cloud.mda-medusa.ru.p12
CERT_PASSWORD=000000000
```

### 3. Добавьте тестовый магазин

Выполните SQL в вашей базе данных:

```sql
INSERT INTO shop_locations (
  id,
  shop_code, 
  shop_name, 
  address, 
  city, 
  phone, 
  working_hours,
  priority_order,
  is_active,
  created_at,
  updated_at
) VALUES 
  (
    'cltest123456789',
    '13', 
    'Магазин Калинина 10', 
    'г. Москва, ул. Калинина, д. 10', 
    'Москва', 
    '+7 (495) 123-45-67', 
    'ПН-ВС 10:00-22:00',
    1,
    true,
    NOW(),
    NOW()
  );
```

### 4. Запустите приложение

```bash
# Backend (в одном терминале)
cd /root/5lb/backend
npm run dev

# Frontend (в другом терминале)
cd /root/5lb/frontend
npm run dev
```

### 5. Проверьте работу

1. Откройте приложение: `http://localhost:5173`
2. Нажмите на вкладку "Каталог"
3. Вы должны увидеть магазин "Калинина 10"
4. Нажмите на магазин чтобы открыть каталог

---

## 📊 Проверка через API

### Проверка списка магазинов
```bash
curl http://localhost:4000/api/catalog/shops
```

### Проверка каталога магазина
```bash
curl http://localhost:4000/api/catalog/shop/13
```

### Запуск обновления каталогов
```bash
curl -X POST http://localhost:4000/api/catalog/update
```

### Проверка статуса
```bash
curl http://localhost:4000/api/catalog/status
```

---

## 🔧 Что делать если не работает?

### Проблема 1: Сертификат не найден

```bash
# Проверьте наличие файла
ls -la /root/5lb/backend/src/certs/terehin_n.cloud.mda-medusa.ru.p12

# Если нет - скопируйте снова
cp terehin_n.cloud.mda-medusa.ru.p12 /root/5lb/backend/src/certs/
chmod 600 /root/5lb/backend/src/certs/terehin_n.cloud.mda-medusa.ru.p12
```

### Проблема 2: Магазин не появляется

```bash
# Проверьте базу данных
psql -d lb_db -c "SELECT * FROM shop_locations WHERE is_active = true;"
```

### Проблема 3: Каталог пустой

1. Проверьте логи backend: `npm run dev` (смотрите вывод в консоли)
2. Убедитесь что магазин с кодом "13" существует в базе fransh-trade
3. Попробуйте запустить обновление вручную: `curl -X POST http://localhost:4000/api/catalog/update`

---

## 📚 Дополнительная документация

Подробная документация: [CATALOG_SYSTEM_DOCUMENTATION.md](./CATALOG_SYSTEM_DOCUMENTATION.md)

---

## ✅ Готово!

Если все работает - система каталогов готова к использованию! 🎉

- Каталоги будут автоматически обновляться каждые 30 минут
- Данные кэшируются на 60 минут для быстрого доступа
- При недоступности API используются данные из базы

---

**Поддержка:** @sysrootix

