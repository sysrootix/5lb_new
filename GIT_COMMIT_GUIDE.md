# Руководство по коммиту и пушу в Git

## ✅ Что МОЖНО и НУЖНО коммитить:

### 📱 Mobile приложение:
- ✅ `mobile/src/` - весь исходный код
- ✅ `mobile/assets/` - иконки и изображения (можно перегенерировать, но удобнее хранить)
- ✅ `mobile/app.json` - конфигурация Expo
- ✅ `mobile/eas.json` - конфигурация EAS Build (безопасен, секретов нет)
- ✅ `mobile/eas.json.example` - пример конфигурации
- ✅ `mobile/package.json` - зависимости
- ✅ `mobile/generate-icons.js` - скрипт генерации иконок
- ✅ `mobile/*.md` - вся документация
- ✅ `mobile/tsconfig.json`, `babel.config.js`, `metro.config.js` - конфигурационные файлы

### 🌐 Frontend:
- ✅ `frontend/src/` - весь исходный код
- ✅ `frontend/public/` - публичные файлы
- ✅ `frontend/package.json` - зависимости
- ✅ Все конфигурационные файлы

### 🔧 Backend:
- ✅ `backend/src/` - весь исходный код
- ✅ `backend/prisma/schema.prisma` - схема БД
- ✅ `backend/prisma/migrations/` - миграции БД
- ✅ `backend/package.json` - зависимости
- ✅ Все конфигурационные файлы

### 📚 Документация:
- ✅ Все `.md` файлы
- ✅ `5lb_logo_RGB-07.svg` - логотип

### ⚙️ Конфигурация:
- ✅ `package.json` (корневой)
- ✅ `tsconfig.base.json`
- ✅ `config/app-config.json` (если нет секретов)
- ✅ `.gitignore`

## ❌ Что НЕ нужно коммитить:

### 🚫 Автоматически игнорируется (.gitignore):
- ❌ `node_modules/` - зависимости (устанавливаются через npm install)
- ❌ `*.env`, `*.env.*` - файлы с секретами
- ❌ `frontend/dist/`, `backend/dist/` - скомпилированные файлы
- ❌ `mobile/android/`, `mobile/ios/` - нативные папки (генерируются)
- ❌ `mobile/.expo/` - временные файлы Expo
- ❌ `*.tsbuildinfo` - файлы кэша TypeScript
- ❌ `.DS_Store`, `.idea/`, `.vscode/` - файлы IDE

### 🚫 Вручную не коммитить:
- ❌ Сертификаты (`.p12`, `.pem`, `.key`, `.crt`)
- ❌ Ключи для подписи приложений (`.jks`, `.keystore`)
- ❌ Файлы с паролями и API ключами
- ❌ Логи (`*.log`)
- ❌ Временные файлы

## 📝 Процесс коммита:

### 1. Проверка статуса:

```bash
git status
```

### 2. Добавление файлов:

```bash
# Добавить все новые и измененные файлы
git add .

# Или выборочно:
git add mobile/
git add frontend/
git add backend/
git add *.md
```

### 3. Проверка что будет закоммичено:

```bash
git status
```

### 4. Создание коммита:

```bash
# По правилам проекта (см. rules.md):
git commit -m "feat: добавлена поддержка мобильных приложений iOS и Android"

# Или для исправлений:
git commit -m "fix: исправлена генерация иконок для мобильного приложения"
```

### 5. Отправка в репозиторий:

```bash
git push origin main
```

Или используйте скрипт из проекта:

```bash
./scripts/git-push.sh "feat: описание изменений"
```

## 🔍 Проверка перед коммитом:

### Убедитесь что не коммитите секреты:

```bash
# Проверка на наличие секретов в файлах
grep -r "password\|secret\|api_key\|token" --include="*.json" --include="*.ts" --include="*.js" . | grep -v node_modules | grep -v ".git"
```

### Проверка размера файлов:

```bash
# Большие файлы (>100MB) могут быть проблемой
find . -type f -size +100M -not -path "./node_modules/*" -not -path "./.git/*"
```

## 📦 Что происходит после пуша:

После `git push` все закоммиченные файлы будут доступны в репозитории GitHub:
- `git@github.com:sysrootix/5lb.git`

На другом компьютере можно будет:

```bash
git clone git@github.com:sysrootix/5lb.git
cd 5lb
npm install  # Установит все зависимости из package.json
```

## 🔄 Обновление на сервере:

После пуша на сервере выполните:

```bash
./scripts/git-pull.sh
```

Или вручную:

```bash
git fetch origin
git pull origin main
```

## ⚠️ Важные замечания:

1. **Не коммитьте `eas.json` с секретами** - текущий файл безопасен, но если добавите секреты, используйте EAS Secrets:
   ```bash
   eas secret:create --scope project --name EXPO_PUBLIC_API_URL --value https://app.5lb.pro/api
   ```

2. **Иконки можно перегенерировать** - они в git для удобства, но при необходимости:
   ```bash
   cd mobile
   npm run generate-icons
   ```

3. **Конфигурация окружения** - используйте `.env` файлы (они в .gitignore) или EAS Secrets для переменных окружения.

## 📋 Чеклист перед коммитом:

- [ ] Проверил `git status` - нет лишних файлов
- [ ] Убедился что нет секретов в коммите
- [ ] Проверил что большие файлы не включены
- [ ] Написал понятное сообщение коммита
- [ ] Готов к push в main ветку

## 🚀 Быстрый коммит всех изменений:

```bash
# Добавить все изменения
git add .

# Проверить что будет закоммичено
git status

# Закоммитить
git commit -m "feat: добавлена поддержка мобильных приложений iOS и Android"

# Отправить в репозиторий
git push origin main
```

Или используйте скрипт:

```bash
./scripts/git-push.sh "feat: добавлена поддержка мобильных приложений iOS и Android"
```

