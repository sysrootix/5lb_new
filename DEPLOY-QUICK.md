# 🚀 Быстрая справка по деплою

## Основные команды

```bash
# Деплой всего проекта (app + crm)
./deploy-all.sh

# Только основное приложение
./deploy-all.sh app

# Только CRM
./deploy-all.sh crm

# Справка
./deploy-all.sh --help
```

## Старые скрипты (обратная совместимость)

```bash
# Работает как раньше (вызывает deploy-all.sh app)
./deploy.sh

# Работает как раньше (вызывает deploy-all.sh crm)
./crm/deploy.sh
```

## После деплоя

```bash
# Проверить статус
pm2 status

# Посмотреть логи основного приложения
pm2 logs 5lb-backend

# Посмотреть логи CRM
pm2 logs 5lb-crm-backend

# Перезапустить вручную
pm2 restart 5lb-backend
pm2 restart 5lb-crm-backend
```

## Подробная документация

См. [DEPLOYMENT.md](./DEPLOYMENT.md)

---
💡 **Рекомендация:** Используйте новый скрипт `deploy-all.sh` для всех деплоев
