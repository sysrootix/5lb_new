# Миграция 5LB - Быстрый старт

## TL;DR - Три простых шага

### На старом сервере:
```bash
cd /root/5lb
./migration-backup.sh
```

### Копируем на новый сервер:
```bash
# Замените NEW_SERVER_IP на IP нового сервера
scp -r /root/5lb-migration-* root@NEW_SERVER_IP:/root/
```

### На новом сервере:
```bash
cd /root/5lb
./migration-restore.sh   # Автоматическая установка и восстановление
./migration-verify.sh    # Проверка работоспособности
```

**⚡ Важно:** На новом сервере автоматически используются порты **60000-60010**:
- Backend: 60000 (было 4000)
- CRM Backend: 60001 (было 5000)

Настраивается автоматически, ничего делать не нужно!

---

## Детальная инструкция

### 📋 Что будет перенесено:

- ✅ База данных PostgreSQL (lb_db)
- ✅ Код проекта (backend, frontend, CRM)
- ✅ Все .env файлы с настройками
- ✅ Конфигурация Nginx
- ✅ Конфигурация PM2

---

## Шаг 1: Резервное копирование (Старый сервер)

```bash
cd /root/5lb
./migration-backup.sh
```

**Что делает:**
- Создает дамп базы данных PostgreSQL
- Архивирует код проекта (без node_modules)
- Сохраняет все .env файлы
- Экспортирует конфигурацию Nginx и PM2

**Результат:**
Директория `/root/5lb-migration-YYYYMMDD-HHMMSS/` со всеми данными

---

## Шаг 2: Копирование на новый сервер

```bash
# Найдите директорию бэкапа
BACKUP_DIR=$(ls -dt /root/5lb-migration-* | head -1)

# Скопируйте на новый сервер (замените IP)
scp -r $BACKUP_DIR root@НОВЫЙ_IP:/root/

# Также скопируйте скрипты миграции
scp migration-restore.sh migration-verify.sh root@НОВЫЙ_IP:/root/5lb/
```

**Альтернатива:** Используйте rsync для больших объемов:
```bash
rsync -avz --progress $BACKUP_DIR root@НОВЫЙ_IP:/root/
```

---

## Шаг 3: Восстановление (Новый сервер)

### 3.1 Автоматическое восстановление

```bash
cd /root/5lb
./migration-restore.sh
```

**Скрипт автоматически:**
- ✅ Установит все зависимости (Node.js, PostgreSQL, PM2, Nginx)
- ✅ Восстановит базу данных
- ✅ Распакует код проекта
- ✅ Скопирует .env файлы
- ✅ Установит npm зависимости
- ✅ Соберет проекты (npm run build)
- ✅ Настроит Nginx
- ✅ Запустит приложения через PM2

**Важно:** Скрипт будет задавать вопросы, отвечайте `y` для продолжения.

### 3.2 Обновите .env файлы

После восстановления проверьте и обновите URL-ы в .env файлах:

```bash
nano /root/5lb/backend/.env
nano /root/5lb/frontend/.env
nano /root/5lb/crm/crm_backend/.env
```

**Что нужно проверить:**
- `APP_DOMAIN` - обновите на новый домен/IP если изменился
- `API_PUBLIC_URL` - обновите на новый URL
- `DATABASE_URL` - обычно остается localhost
- Другие URL-ы и домены

---

## Шаг 4: Проверка работоспособности

```bash
cd /root/5lb
./migration-verify.sh
```

**Что проверяет:**
- ✅ Системные службы (PostgreSQL, Nginx)
- ✅ База данных и данные в ней
- ✅ PM2 процессы
- ✅ Файловая структура
- ✅ Открытые порты
- ✅ Работу API
- ✅ Логи на ошибки

**Возможные результаты:**
- ✅ **Все зеленое** - отлично, можно переключать DNS
- ⚠️  **Предупреждения** - некритично, но лучше исправить
- ❌ **Ошибки** - нужно исправить перед использованием

---

## Шаг 5: Ручное тестирование

Проверьте функциональность:

### 5.1 Проверка API
```bash
curl http://localhost:4000/api/health
```

### 5.2 Проверка сайта
```bash
# Через IP
curl -I http://НОВЫЙ_IP

# Через домен (если DNS уже переключен)
curl -I https://app.5lb.pro
```

### 5.3 Проверка в браузере

1. Откройте `http://НОВЫЙ_IP` в браузере
2. Попробуйте:
   - Авторизацию/регистрацию
   - Просмотр каталога
   - Добавление в корзину
   - Просмотр профиля
3. Проверьте CRM панель

---

## Шаг 6: Настройка SSL (опционально)

```bash
# Установка сертификата Let's Encrypt
certbot --nginx -d app.5lb.pro

# Проверка автообновления
certbot renew --dry-run
```

---

## Шаг 7: Переключение DNS

После успешной проверки:

1. Зайдите в панель управления доменом
2. Обновите A-запись для `app.5lb.pro`:
   - Старый IP → Новый IP
3. Сохраните изменения
4. Подождите 5-30 минут для распространения DNS

**Проверка DNS:**
```bash
nslookup app.5lb.pro
dig app.5lb.pro
```

---

## Шаг 8: Финальные настройки

### 8.1 Автозапуск PM2
```bash
pm2 startup
# Выполните команду которую выведет pm2 startup
pm2 save
```

### 8.2 Настройка файрвола
```bash
ufw allow 22/tcp    # SSH
ufw allow 80/tcp    # HTTP
ufw allow 443/tcp   # HTTPS
ufw enable
```

### 8.3 Мониторинг логов
```bash
# Установка ротации логов PM2
pm2 install pm2-logrotate
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 30
```

---

## 📊 Чеклист миграции

```
[ ] 1. Создан бэкап на старом сервере (migration-backup.sh)
[ ] 2. Бэкап скопирован на новый сервер (scp)
[ ] 3. Запущено восстановление (migration-restore.sh)
[ ] 4. Обновлены .env файлы
[ ] 5. Пройдена автоматическая проверка (migration-verify.sh)
[ ] 6. Проведено ручное тестирование
[ ] 7. Настроен SSL (certbot)
[ ] 8. Переключен DNS
[ ] 9. Настроен автозапуск PM2
[ ] 10. Настроен файрвол
[ ] 11. Все работает стабильно 24+ часа
[ ] 12. Можно отключать старый сервер
```

---

## 🆘 Решение проблем

### PM2 процессы не запускаются
```bash
pm2 logs
pm2 restart all
```

### База данных не подключается
```bash
systemctl status postgresql
psql -U lb_user -h localhost -d lb_db
```

### Nginx показывает 502
```bash
pm2 list  # Проверить что backend работает
nginx -t  # Проверить конфигурацию
tail -f /var/log/nginx/error.log
```

### API не отвечает
```bash
pm2 logs 5lb-backend
curl http://localhost:4000/api/health
```

---

## 📚 Дополнительная документация

- **Полное руководство:** `MIGRATION-GUIDE.md`
- **Troubleshooting:** См. раздел в MIGRATION-GUIDE.md

---

## ⚠️  Важные напоминания

1. **Не удаляйте старый сервер** пока новый не работает стабильно минимум 48 часов
2. **Протестируйте все функции** перед переключением DNS
3. **Сохраните бэкапы** на случай проблем
4. **Проверяйте логи** регулярно первые дни после миграции

---

## 🚀 Готово!

После выполнения всех шагов ваше приложение 5LB будет полностью перенесено на новый сервер.

Если возникли проблемы - см. **MIGRATION-GUIDE.md** раздел **Troubleshooting**.
