5LB - Telegram Web App Store

Технологический стек:
Frontend: React Native + React для мобильных и веб-версий
Backend: Node.js + Express
База данных: PostgreSQL

⸻

⚠️ ВАЖНЫЕ ПРАВИЛА ДЛЯ РАЗРАБОТЧИКОВ

1. КОММИТЫ В GIT - После каждого добавления новой функции или исправления бага делай коммит:
git add .
git commit -m "feat: описание изменения"  # для добавления новой фичи
git commit -m "fix: описание исправления" # для багфиксов
git push origin main
   •	Можно использовать скрипт `scripts/git-push.sh "feat: описание"` для автоматизации пушей.

2. ОБНОВЛЕНИЕ КОДА НА СЕРВЕРЕ - Используй `scripts/git-pull.sh` (выполняет fetch + fast-forward pull).

3. ДОМЕН И HTTPS - Все сервисы должны указывать боевой домен и базовый API URL в `config/app-config.json` и `.env` файлах (`APP_DOMAIN`, `API_PUBLIC_URL`, `CORS_ALLOWED_ORIGINS`, `VITE_API_URL`, `EXPO_PUBLIC_API_URL`).

4. МИГРАЦИИ БАЗЫ ДАННЫХ - Если потребуется миграция для базы данных, создавай файл .md с командами для выполнения на сервере:
# Создать файл migration-YYYY-MM-DD-описание.md с такими командами:
# 1. cd backend
# 2. npx prisma migrate dev --name название_миграции
# 3. Проверить изменения в БД

🎯 О ПРОЕКТЕ

5LB — это веб- и мобильное приложение для магазина спортивного питания с интеграцией Telegram авторизации, каталогом товаров, корзиной, системой лояльности и профилем пользователя. Приложение должно работать как на мобильных устройствах (через React Native), так и на веб-платформе (через React).

⸻

🔥 ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

1. Регистрация и вход по номеру телефона с отправкой и подтверждением SMS-кода.
2. Авторизация через Telegram (WebApp и бот) с синхронизацией профиля.
3. Профиль пользователя с возможностью просмотра, редактирования и выхода.
4. Каталог с товарами и категориями, корзина, управление заказами и бонусами.
5. Поддержка Android, iOS и веб-версии, адаптивный UI на оранжево-бело-красной палитре.
6. Единый backend с JWT-авторизацией, интеграцией SMS-провайдера и Telegram API.

⸻

🛠 ТЕХНОЛОГИЧЕСКИЙ СТЕК

Frontend
	•	React Native — для мобильных приложений (iOS/Android)
	•	React — для веб-версии
	•	TypeScript 5.3 — строгая типизация
	•	Vite 5 — сборка и запуск для быстрого старта
	•	Tailwind CSS 3.4 — стилизация с использованием кастомных цветов 5LB
	•	Zustand 4.4 — для управления состоянием (авторизация и данные пользователя)
	•	React Query 5.15 — для кеширования данных и работы с API
	•	React Router 6.21 — для маршрутизации
	•	React Navigation 6 — для мобильной навигации
	•	Framer Motion 10.16 — для анимаций
	•	Lucide React — иконки
	•	React Hot Toast — уведомления
	•	Axios 1.6 — HTTP клиент
	•	React Native Paper / Native Base — готовые UI-компоненты
	•	Telegram WebApp API — интеграция с Telegram для авторизации

Backend
	•	Node.js 20+ + Express 4.18 + TypeScript 5.3 — для серверной части
	•	PostgreSQL 16 + Prisma 5.7 — база данных и ORM
	•	JWT — для авторизации пользователей
	•	node-telegram-bot-api — для интеграции с Telegram Bot
	•	SigmaSMS — отправка SMS кодов подтверждения
	•	Winston — для логирования
	•	Helmet + CORS + Rate Limiting — для обеспечения безопасности

DevOps
	•	PM2 — для управления процессами
	•	Nginx — веб-сервер для продакшн окружения (шаблон: `deploy/nginx.conf`)
	•	npm workspaces — монорепозиторий для frontend и backend

SMS
	•	SigmaSMS API — HTTP Basic auth (login/password), endpoint `/api/sendings`, параметр отправителя `SIGMA_SMS_SENDER`.

⸻

📁 СТРУКТУРА ПРОЕКТА

5LB/
├── mobile/                # React Native приложение
│   ├── src/
│   │   ├── screens/       # Экраны (Login, VerifyCode, Register, Home, Profile)
│   │   ├── navigation/    # Стек и таб-навигация (React Navigation)
│   │   ├── components/    # Универсальные RN-компоненты
│   │   ├── store/         # Zustand сторы (authStore, profileStore)
│   │   └── services/      # Работа с API и push/SMS
│   └── app.json           # Expo/CLI конфигурация
├── frontend/              # React приложение
│   ├── src/
│   │   ├── api/          # API клиенты (auth, product, cart, user, bonus)
│   │   ├── pages/        # Страницы (Dashboard, Catalog, Cart, Profile, etc.)
│   │   ├── components/   # Компоненты (BottomNav, ProductOptionsModal, etc.)
│   │   ├── store/        # Zustand хранилища (authStore)
│   │   ├── hooks/        # Хуки (useTelegramApp)
│   │   ├── index.css     # Tailwind + кастомные цвета 5LB
│   │   └── App.tsx       # Главный компонент с роутингом
│   ├── vite.config.ts    # Конфигурация Vite с proxy
│   └── tailwind.config.js # Кастомные цвета 5LB
├── backend/              # Express сервер
│   ├── src/
│   │   ├── controllers/  # Обработчики (auth, user, product, cart, bonus)
│   │   ├── routes/       # Express маршруты
│   │   ├── middleware/   # Middleware (auth, errorHandler)
│   │   ├── services/     # Интеграции (smsService, telegramService)
│   │   ├── config/       # Конфиг (database, telegram, logger, sms)
│   │   └── index.ts      # Инициализация Express
│   └── prisma/
│       └── schema.prisma # Схема БД
└── package.json          # Root workspace конфигурация

🌐 ДОМЕН И HTTPS
	•	`config/app-config.json` — единый источник домена, публичного API URL и списка допустимых origin (отредактируй перед деплоем).
	•	`.env` файлы: `APP_DOMAIN`, `API_PUBLIC_URL`, `CORS_ALLOWED_ORIGINS`, `VITE_API_URL`, `EXPO_PUBLIC_API_URL`.
	•	`deploy/nginx.conf` — шаблон конфига Nginx (замени `<DOMAIN>` и пути к сертификатам).

🎨 ДИЗАЙН СИСТЕМА

5LB Цветовая схема

Проект использует кастомную цветовую схему 5LB для адаптации под брендинг и поддержания консистентности UI:

// tailwind.config.js
colors: {
  '5lb-bg': '#FF7F32',        // Оранжевый фон
  '5lb-text': '#FFFFFF',      // Белый текст
  '5lb-button': '#E94B3C',    // Красная кнопка
  '5lb-link': '#FF7F32',      // Оранжевая ссылка
  '5lb-hint': '#FF9B56',      // Светлый оранжевый (для подсказок и акцентов)
}

Tailwind Custom Colors

// tailwind.config.js
colors: {
  '5lb-bg': '#FF7F32',
  '5lb-text': '#FFFFFF',
  '5lb-button': '#E94B3C',
  '5lb-link': '#FF7F32',
  '5lb-hint': '#FF9B56',
}


⸻

🔑 ОСНОВНЫЕ ФИЧИ
	1.	Telegram аутентификация — автоматический вход через Telegram WebApp
	2.	Каталог товаров — фильтрация, поиск, варианты товаров
	3.	Корзина — добавление/удаление, выбор характеристик, подсчет итога
	4.	Программа лояльности — начисление/списание бонусов
	5.	Профиль пользователя — информация из Telegram, обновление данных
	6.	История заказов — отслеживание статуса, использованные бонусы

⸻

🚀 РАЗРАБОТКА

Build команды

# Сборка всего проекта (frontend + backend)
npm run build

# Сборка только frontend
npm run build:frontend

# Сборка только backend
npm run build:backend

Проверка кода

# TypeScript проверка
npm run build  # Покажет все ошибки типизации

# Линтинг
npm run lint

База данных

⚠️ НЕ ВЫПОЛНЯЙ эти команды — только создавай .md файл с инструкциями!

Если нужна миграция, создай файл migration-[дата]-[описание].md с такими командами:

# Миграция: [Описание изменений]

## Команды для выполнения на сервере:

1. Перейти в директорию backend:
   ```bash
   cd backend

   	2.	Применить миграцию:

    npx prisma migrate dev --name название_миграции
    	3.	Сгенерировать Prisma Client:
        npx prisma generate

        	4.	Проверить изменения:

            npx prisma studio


Изменения в схеме:

[Описание того, что изменилось в schema.prisma]

### Структура на сервере

/var/www/5lb/        # Frontend (dist файлы)
/home/user/shop/     # Backend + исходники

### Процесс деплоя
1. Коммит изменений: `git add . && git commit -m "feat: описание"`
2. Пуш на GitHub: `git push`
3. На сервере: запусти скрипт `./deploy.sh`

---

## 📡 API Endpoints

**Base URL**: `https://app.5lb.pro/api`

### Authentication
- `POST /auth/login` — инициировать вход по телефону и отправить SMS код
- `POST /auth/verify` — подтвердить SMS код и выдать JWT
- `POST /auth/telegram` — вход через Telegram WebApp
- `POST /auth/logout` — завершить сессию пользователя
- `GET /auth/verify` — проверка действительности JWT токена

### Products
- `GET /categories` — список категорий
- `GET /products` — список товаров (фильтры: categoryId, search, featured)
- `GET /products/:id` — детали товара

### Cart
- `GET /cart` — получить корзину
- `POST /cart/add` — добавить товар
- `PUT /cart/:id` — обновить количество
- `DELETE /cart/:id` — удалить товар
- `DELETE /cart` — очистить корзину

### User
- `GET /users/profile` — профиль пользователя
- `PUT /users/profile` — обновить профиль

### Bonus
- `GET /bonus` — баланс и история бонусов
- `POST /bonus/calculate` — расчет бонусов для заказа

---

## 🐛 ОТЛАДКА

### Frontend ошибки
```bash
# Проверка TypeScript
cd frontend
npm run build

# Логи в браузере
# Telegram WebApp API errors будут в console

🎨 UI/UX Guidelines

Карточки товаров
	•	Единообразное расположение кнопок: все кнопки (+, -, “Добавить”) должны быть на одинаковых позициях
	•	Плейсхолдер для изображений: товары без фото показывают дефолтное изображение
	•	Hover эффекты: плавные переходы при наведении
	•	Анимации: Framer Motion для появления/исчезновения

Цветовая схема
	•	Используй только 5LB переменные (5lb-* классы)
	•	Базовая палитра: оранжевый (#FF6B00), белый (#FFFFFF), акцентный красный (#D7263D)
	•	Не хардкодь цвета - приложение должно адаптироваться к теме пользователя
	•	Для акцентов используй 5lb-button и 5lb-link


🚨 Важные правила
	1.	❌ НЕ запускай npm run dev в продакшн окружении
	2.	❌ НЕ выполняй команды миграций БД - только создавай .md файлы с инструкциями
	3.	✅ Всегда делай коммит после изменений - git add . && git commit -m "feat: описание"
	4.	✅ Используй npm run build для проверки кода
	5.	✅ Тестируй в Telegram WebApp - не только в браузере
	6.	✅ Проверяй темную тему - используй Telegram переменные
	7.	✅ Единообразие UI - кнопки на одинаковых местах
	8.	✅ Плейсхолдеры - для товаров без изображений
	9.	✅ TypeScript строгий - исправляй все ошибки типов
