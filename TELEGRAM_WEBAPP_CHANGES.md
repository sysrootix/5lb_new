# Изменения в логике входа через Telegram WebApp

## Дата: 28 октября 2025

## Описание изменений

### 1. Автоматический вход через Telegram WebApp

**Было:** При открытии Telegram WebApp автоматически проверялся `chat_id` в базе, и если пользователь не найден, происходило перенаправление на страницу `/login` с уведомлением.

**Стало:** 
- Если пользователь с `chat_id` найден → автоматический вход в аккаунт
- Если пользователь с `chat_id` НЕ найден → приложение просто открывается без перенаправлений
- Пользователь сам решает, войти или зарегистрироваться

**Файл:** `frontend/src/App.tsx`

**Изменения:**
```typescript
// Раньше при ошибке TELEGRAM_USER_NOT_FOUND:
navigate('/login', { replace: true });
toast('Войдите через Telegram для регистрации', { icon: '👋', duration: 4000 });

// Теперь:
console.log('Telegram user not found, opening app as guest');
// Приложение просто открывается
```

### 2. Логика входа через кнопку "Войти через Telegram" в WebApp

**Логика:**
1. Пользователь нажимает кнопку "Войти через Telegram"
2. Если `chat_id` найден в базе → вход выполнен
3. Если `chat_id` НЕ найден:
   - Запрашивается номер телефона через `window.Telegram.WebApp.requestContact()`
   - После получения телефона → перенаправление на `/complete-registration`
   - Пользователь заполняет ФИО, дату рождения и другие данные

**Файл:** `frontend/src/pages/Login.tsx` (строки 310-344)

### 3. Исправление проблемы со скроллом на странице Login

**Проблема:** Страница Login проматывалась вниз при переходе вместо того, чтобы быть наверху.

**Решение:**
- Добавлен принудительный скролл наверх при монтировании компонента
- Добавлен скролл наверх при смене шага (phone → code)
- Используется несколько методов для надежности:
  - `window.scrollTo({ top: 0, behavior: 'instant' })`
  - `document.documentElement.scrollTop = 0`
  - `document.body.scrollTop = 0`

**Файл:** `frontend/src/pages/Login.tsx` (строки 45-67)

## Проверка изменений

### 1. Сценарий: Новый пользователь в Telegram WebApp

1. Откройте Telegram WebApp
2. Вы увидите главную страницу (не будет перенаправления на `/login`)
3. Нажмите "Войти через Telegram" на странице Login
4. Telegram запросит номер телефона
5. После предоставления телефона → перенаправление на `/complete-registration`
6. Заполните форму регистрации

### 2. Сценарий: Существующий пользователь в Telegram WebApp

1. Откройте Telegram WebApp
2. Произойдет автоматический вход
3. Вы попадете на главную страницу

### 3. Сценарий: Проверка скролла

1. Откройте страницу Login
2. Страница должна быть наверху (не прокручена вниз)
3. Введите номер телефона и нажмите "Позвонить мне"
4. Страница с вводом кода также должна быть наверху

## Затронутые файлы

### Frontend
- ✅ `frontend/src/App.tsx` - изменена логика обработки ошибки TELEGRAM_USER_NOT_FOUND
- ✅ `frontend/src/pages/Login.tsx` - исправлен скролл и проверена логика запроса телефона
- ✅ `frontend/src/pages/CompleteRegistration.tsx` - защита от повторной регистрации (из предыдущего исправления)

### Документация
- ✅ `TELEGRAM_WEBAPP_CHANGES.md` - этот файл
- ✅ `TELEGRAM_LOGIN_FIX.md` - документация предыдущих исправлений
- ✅ `ИСПРАВЛЕНИЕ_ВХОДА_TELEGRAM.md` - краткая инструкция на русском

## Обратная совместимость

Все изменения обратно совместимы:
- Старая логика входа через SMS работает без изменений
- Вход через Telegram в браузере (не WebApp) работает как раньше
- Новые пользователи могут зарегистрироваться через телефон или через Telegram
- Существующие пользователи могут войти любым способом

## Известные ограничения

1. **Запрос телефона в Telegram WebApp**: 
   - Метод `window.Telegram.WebApp.requestContact()` может быть недоступен в старых версиях Telegram
   - Если метод недоступен, пользователь просто перенаправляется на `/complete-registration` без автоматического получения телефона

2. **Перезапуск WebApp после получения контакта**:
   - После того как пользователь предоставит номер телефона, Telegram может перезапустить WebApp
   - Данные телефона придут в `initDataUnsafe` и будут обработаны автоматически

## Рекомендации

1. Протестируйте вход через Telegram WebApp на реальном устройстве
2. Проверьте, что запрос телефона работает корректно
3. Убедитесь, что скролл на странице Login работает правильно на разных устройствах
4. Проверьте логи в консоли браузера при тестировании

## Дополнительная информация

Для более подробной технической информации см.:
- `TELEGRAM_LOGIN_FIX.md` - исправление проблемы с `isRegistrationComplete`
- `AUTH_SYSTEM_COMPLETE.md` - полная документация по системе аутентификации

