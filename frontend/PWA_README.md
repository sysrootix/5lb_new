# PWA (Progressive Web App) - Документация

## ✅ Что настроено

### 1. **Манифест PWA**
- Название: **5LB — Магазин спортивного питания**
- Короткое название: **5LB**
- Цвет темы: `#FF7F32` (оранжевый)
- Фоновый цвет: `#FFFFFF` (белый)
- Режим отображения: `standalone` с fallback на `fullscreen`
- **Полноэкранный режим**: Приложение открывается в полный экран без браузерных элементов
- Ориентация: `portrait`

### 2. **Иконки**
Созданы все необходимые иконки для разных устройств:
- `pwa-192x192.png` - Стандартная иконка 192×192
- `pwa-512x512.png` - Стандартная иконка 512×512
- `pwa-maskable-192x192.png` - Maskable иконка 192×192 (для адаптивных форм)
- `pwa-maskable-512x512.png` - Maskable иконка 512×512
- `apple-touch-icon.png` - Иконка для iOS 180×180
- `favicon-32x32.png` - Favicon 32×32
- `favicon-16x16.png` - Favicon 16×16
- `favicon.ico` - Стандартный favicon

### 3. **Service Worker**
- Автоматическое обновление (registerType: 'autoUpdate')
- Кэширование статических ресурсов
- Кэширование Google Fonts на 1 год
- Кэширование API запросов на 5 минут
- Работает даже в режиме разработки

### 4. **Промпт установки**
Компонент `InstallPWAPrompt` автоматически предлагает:
- **Android/Desktop**: Нативный промпт установки через браузер
- **iOS**: Инструкция по установке через Safari (Share → На экран Домой)
- **Telegram WebApp**: Промпт НЕ показывается
- Показывается через 3 секунды после загрузки
- Показывается только при первом посещении

### 5. **Meta теги**
- Полная поддержка PWA meta тегов
- Apple Mobile Web App теги с `black-translucent` status bar
- Open Graph для социальных сетей
- Twitter Card теги
- Viewport с поддержкой notch (viewport-fit: cover)

### 6. **Полноэкранный режим**
- Автоматическая адаптация под разные устройства
- Поддержка iOS notch и safe-area
- Правильная прокрутка в PWA режиме
- Оптимизация высоты экрана для iOS (-webkit-fill-available)
- Отключение overscroll эффектов
- Специальные CSS стили для `display-mode: standalone`

## 🚀 Использование

### Генерация иконок
Если нужно пересоздать иконки:
```bash
npm run generate-icons
```

### Запуск в режиме разработки
```bash
npm run dev
```
PWA будет работать даже в dev режиме!

### Сборка для продакшена
```bash
npm run build
```

### Превью продакшен сборки
```bash
npm run preview
```

## 📱 Тестирование PWA

### Chrome/Edge (Desktop)
1. Откройте DevTools (F12)
2. Вкладка "Application" → "Manifest"
3. Проверьте все поля манифеста
4. Вкладка "Service Workers" - должен быть активен
5. В адресной строке справа появится иконка установки ⊕

### Chrome/Edge (Android)
1. Откройте сайт в браузере
2. Через 3 секунды появится промпт установки
3. Или в меню браузера → "Установить приложение"

### Safari (iOS)
1. Откройте сайт в Safari
2. Через 3 секунды появится инструкция
3. Нажмите кнопку "Поделиться" (квадрат со стрелкой)
4. Выберите "На экран Домой"
5. Нажмите "Добавить"

**Важно для iOS:** После установки приложение откроется в полноэкранном режиме с черной прозрачной статус-баром, что обеспечивает максимальное использование экрана.

## 🔧 Настройки

### Изменение темы приложения
В `vite.config.ts` → `VitePWA` → `manifest`:
```typescript
theme_color: '#FF7F32',  // Цвет темы
background_color: '#FFFFFF',  // Фон при загрузке
```

### Изменение кэширования
В `vite.config.ts` → `VitePWA` → `workbox` → `runtimeCaching`:
```typescript
{
  urlPattern: /\/api\/.*/i,
  handler: 'NetworkFirst',  // Стратегия кэширования
  options: {
    cacheName: 'api-cache',
    expiration: {
      maxEntries: 50,
      maxAgeSeconds: 60 * 5  // 5 минут
    }
  }
}
```

### Отключение промпта установки
Удалите `<InstallPWAPrompt />` из `src/App.tsx`

## 📊 Проверка PWA качества

### Lighthouse (в Chrome DevTools)
1. F12 → вкладка "Lighthouse"
2. Выберите категорию "Progressive Web App"
3. Нажмите "Analyze page load"
4. Должно быть 100/100 баллов

### PWA Builder
Откройте https://www.pwabuilder.com/ и введите URL вашего сайта

## 🎨 Кастомизация иконок

Отредактируйте `generate-icons.cjs`:
- Измените градиент (строки 17-18)
- Измените текст "5LB" (строка 30)
- Измените размеры и стили
- Запустите `npm run generate-icons`

## ✨ Особенности

1. **Работает офлайн** - Service Worker кэширует все необходимое
2. **Быстрая загрузка** - Статика кэшируется локально
3. **Как нативное приложение** - Полноэкранный режим, своя иконка
4. **Автообновление** - Новая версия применяется автоматически
5. **SEO оптимизация** - Все meta теги для социальных сетей
6. **iOS поддержка** - Специальные Apple meta теги и иконки
7. **Умный промпт** - Не показывается в Telegram WebApp

## 🐛 Troubleshooting

### PWA не устанавливается
- Проверьте, что сайт открыт по HTTPS (или localhost)
- Проверьте наличие manifest.json в Network панели
- Проверьте Service Worker в DevTools → Application

### Иконки не отображаются
- Запустите `npm run generate-icons`
- Проверьте папку `public/` на наличие всех PNG файлов
- Очистите кэш браузера (Ctrl+Shift+Delete)

### Промпт не появляется
- Очистите localStorage: `localStorage.removeItem('pwa-install-prompt-seen')`
- Перезагрузите страницу
- Проверьте, что вы не в Telegram WebApp

### PWA не открывается в полный экран
- Удалите приложение с главного экрана
- Очистите кэш браузера
- Переустановите приложение
- На iOS убедитесь, что открываете из иконки на главном экране, а не из Safari
- Проверьте в DevTools → Application → Manifest, что `display` установлен как `standalone`

### Service Worker не регистрируется
- Проверьте консоль на ошибки
- Убедитесь, что сайт на HTTPS
- Пересоберите проект: `npm run build`

