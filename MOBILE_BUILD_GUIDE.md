# Руководство по сборке iOS и Android приложений

Это руководство поможет вам собрать и опубликовать мобильное приложение 5LB для iOS и Android.

## Содержание
1. [Предварительные требования](#предварительные-требования)
2. [Настройка проекта](#настройка-проекта)
3. [Подготовка иконок и splash-экранов](#подготовка-иконок-и-splash-экранов)
4. [Сборка для Android](#сборка-для-android)
5. [Сборка для iOS](#сборка-для-ios)
6. [Публикация в магазины](#публикация-в-магазины)

---

## Предварительные требования

### Для Android:
- **Node.js** (версия 18 или выше)
- **Java Development Kit (JDK)** версии 17 или выше
- **Android Studio** с установленным Android SDK
- **Expo CLI**: `npm install -g expo-cli eas-cli`
- Аккаунт в **Expo** (бесплатный)

### Для iOS:
- **macOS** (обязательно!)
- **Xcode** (последняя версия из App Store)
- **Node.js** (версия 18 или выше)
- **Expo CLI**: `npm install -g expo-cli eas-cli`
- **Для тестирования на симуляторе:** Бесплатный Apple ID (достаточно!)
- **Для тестирования на реальном устройстве и публикации:** Apple Developer Account ($99/год)

> 💡 **Важно:** Для разработки и тестирования на симуляторе iOS платный аккаунт НЕ нужен! Подробнее см. [mobile/IOS_TESTING_GUIDE.md](mobile/IOS_TESTING_GUIDE.md)

---

## Настройка проекта

### 1. Установка зависимостей

```bash
cd mobile
npm install
```

### 2. Установка EAS CLI (Expo Application Services)

```bash
npm install -g eas-cli
```

### 3. Вход в Expo

```bash
eas login
```

Если у вас нет аккаунта, создайте его:
```bash
eas register
```

### 4. Инициализация EAS Build

```bash
cd mobile
eas build:configure
```

Это создаст файл `eas.json` с конфигурацией сборок.

---

## Подготовка иконок и splash-экранов

### Создание папки assets

```bash
cd mobile
mkdir -p assets
```

### Требования к иконкам:

**Для iOS:**
- `icon.png` - 1024x1024 px (без прозрачности, PNG)

**Для Android:**
- `adaptive-icon.png` - 1024x1024 px (центрированная иконка на прозрачном фоне)

**Splash screen:**
- `splash.png` - 1242x2436 px (или 2048x2732 px для планшетов)

### Быстрое создание иконок

Вы можете использовать онлайн-инструменты:
- [Expo Icon Generator](https://www.npmjs.com/package/@expo/image-utils)
- [App Icon Generator](https://www.appicon.co/)

Или создать вручную из вашего логотипа `5lb_logo_RGB-07.svg`.

---

## Сборка для Android

### Вариант 1: Локальная сборка (требует Android Studio)

```bash
cd mobile
npm run android
```

Это запустит Metro bundler и откроет Android эмулятор или подключенное устройство.

### Вариант 2: Сборка APK через EAS Build (рекомендуется)

#### 2.1. Настройка eas.json

Убедитесь, что в `eas.json` есть конфигурация:

```json
{
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "preview": {
      "distribution": "internal",
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "android": {
        "buildType": "apk"
      }
    }
  }
}
```

#### 2.2. Сборка APK

```bash
# Для тестирования (preview build)
eas build --platform android --profile preview

# Для продакшена
eas build --platform android --profile production
```

#### 2.3. Сборка AAB (для Google Play Store)

Для публикации в Google Play нужен AAB формат:

```bash
eas build --platform android --profile production
```

Или укажите в `eas.json`:
```json
{
  "build": {
    "production": {
      "android": {
        "buildType": "app-bundle"
      }
    }
  }
}
```

### Вариант 3: Сборка локально через EAS

```bash
eas build --platform android --local
```

Требует установленного Android SDK и JDK.

---

## Сборка для iOS

⚠️ **Важно:** Сборка для iOS возможна только на macOS!

### Вариант 1: Через EAS Build (рекомендуется)

#### 1.1. Настройка Apple Developer Account

1. Зарегистрируйтесь на [developer.apple.com](https://developer.apple.com)
2. Оплатите годовой взнос ($99)
3. Создайте App ID в Apple Developer Portal

#### 1.2. Настройка сертификатов

EAS может автоматически управлять сертификатами:

```bash
eas credentials
```

Выберите платформу iOS и следуйте инструкциям.

#### 1.3. Сборка IPA

```bash
# Для тестирования (TestFlight)
eas build --platform ios --profile preview

# Для продакшена (App Store)
eas build --platform ios --profile production
```

### Вариант 2: Локальная сборка

```bash
cd mobile
npm run ios
```

Это запустит Metro bundler и откроет iOS симулятор (требует Xcode).

### Вариант 3: Через Xcode (продвинутый)

1. Сгенерируйте нативный проект:
```bash
cd mobile
npx expo prebuild
```

2. Откройте `ios/5lb.xcworkspace` в Xcode
3. Выберите целевое устройство и нажмите Run

---

## Публикация в магазины

### Публикация в Google Play Store

#### 1. Подготовка

1. Создайте аккаунт разработчика в [Google Play Console](https://play.google.com/console) ($25 единоразово)
2. Соберите AAB файл (см. раздел "Сборка для Android")

#### 2. Загрузка приложения

1. Войдите в Google Play Console
2. Создайте новое приложение
3. Заполните информацию о приложении:
   - Название: "5LB"
   - Краткое описание
   - Полное описание
   - Скриншоты (минимум 2)
   - Иконка приложения
   - Графические ресурсы

#### 3. Загрузка AAB

1. Перейдите в раздел "Production" → "Create new release"
2. Загрузите AAB файл (скачайте его после сборки через EAS)
3. Заполните примечания к релизу
4. Отправьте на проверку

#### 4. Автоматическая публикация через EAS

```bash
eas submit --platform android
```

EAS автоматически загрузит приложение в Google Play Console.

---

### Публикация в Apple App Store

#### 1. Подготовка

1. Убедитесь, что у вас есть Apple Developer Account ($99/год)
2. Создайте App ID в [Apple Developer Portal](https://developer.apple.com/account)
3. Соберите IPA файл (см. раздел "Сборка для iOS")

#### 2. Настройка App Store Connect

1. Войдите в [App Store Connect](https://appstoreconnect.apple.com)
2. Создайте новое приложение:
   - Bundle ID (должен совпадать с `expo.ios.bundleIdentifier` в app.json)
   - Название: "5LB"
   - Основной язык
   - SKU (уникальный идентификатор)

#### 3. Заполнение информации

1. Скриншоты для разных размеров экранов (обязательно!)
2. Описание приложения
3. Ключевые слова
4. Категория
5. Контактная информация
6. Информация о конфиденциальности

#### 4. Загрузка через TestFlight (для тестирования)

```bash
eas build --platform ios --profile preview
eas submit --platform ios
```

Или через Xcode:
1. Откройте Xcode → Window → Organizer
2. Выберите архив → Distribute App
3. Выберите "App Store Connect"
4. Загрузите в TestFlight

#### 5. Отправка на модерацию

После успешной загрузки в TestFlight:
1. Перейдите в App Store Connect
2. Выберите ваше приложение → "TestFlight"
3. После успешного тестирования перейдите в "App Store"
4. Заполните всю необходимую информацию
5. Нажмите "Submit for Review"

#### 6. Автоматическая публикация через EAS

```bash
eas submit --platform ios
```

---

## Конфигурация app.json

Убедитесь, что `app.json` содержит всю необходимую информацию:

```json
{
  "expo": {
    "name": "5LB",
    "slug": "5lb",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "automatic",
    "splash": {
      "image": "./assets/splash.png",
      "resizeMode": "contain",
      "backgroundColor": "#FF6B00"
    },
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "com.yourcompany.5lb",
      "buildNumber": "1"
    },
    "android": {
      "adaptiveIcon": {
        "foregroundImage": "./assets/adaptive-icon.png",
        "backgroundColor": "#FFFFFF"
      },
      "package": "com.yourcompany.5lb",
      "versionCode": 1
    },
    "extra": {
      "eas": {
        "projectId": "your-project-id"
      }
    }
  }
}
```

---

## Переменные окружения

Создайте файл `.env` в папке `mobile/`:

```env
EXPO_PUBLIC_API_URL=https://app.5lb.pro/api
```

Или используйте EAS Secrets:

```bash
eas secret:create --scope project --name EXPO_PUBLIC_API_URL --value https://app.5lb.pro/api
```

---

## Обновление приложения

### Для Android (Google Play):

1. Увеличьте `versionCode` в `app.json`:
```json
{
  "android": {
    "versionCode": 2
  }
}
```

2. Увеличьте версию:
```json
{
  "expo": {
    "version": "1.0.1"
  }
}
```

3. Соберите новую версию:
```bash
eas build --platform android --profile production
```

4. Загрузите в Google Play Console

### Для iOS (App Store):

1. Увеличьте `buildNumber` в `app.json`:
```json
{
  "ios": {
    "buildNumber": "2"
  }
}
```

2. Увеличьте версию:
```json
{
  "expo": {
    "version": "1.0.1"
  }
}
```

3. Соберите новую версию:
```bash
eas build --platform ios --profile production
```

4. Загрузите через EAS Submit или Xcode

---

## Полезные команды

```bash
# Проверка конфигурации
eas build:configure

# Просмотр статуса сборки
eas build:list

# Скачать собранное приложение
eas build:download

# Открыть страницу сборки в браузере
eas build:view

# Настройка credentials
eas credentials

# Обновление приложения через OTA (без пересборки)
eas update --branch production --message "Bug fixes"
```

---

## Решение проблем

### Android:

**Проблема:** "SDK location not found"
**Решение:** Установите Android Studio и настройте переменную окружения `ANDROID_HOME`

**Проблема:** "JDK not found"
**Решение:** Установите JDK 17+ и настройте `JAVA_HOME`

### iOS:

**Проблема:** "No provisioning profile found"
**Решение:** Запустите `eas credentials` и настройте сертификаты

**Проблема:** "Code signing error"
**Решение:** Убедитесь, что Bundle ID совпадает в app.json и Apple Developer Portal

---

## Дополнительные ресурсы

- [Expo Documentation](https://docs.expo.dev/)
- [EAS Build Documentation](https://docs.expo.dev/build/introduction/)
- [EAS Submit Documentation](https://docs.expo.dev/submit/introduction/)
- [React Native Documentation](https://reactnative.dev/docs/getting-started)

---

## Поддержка

Если возникли проблемы, проверьте:
1. Логи сборки в Expo Dashboard
2. Документацию Expo
3. Форум Expo: https://forums.expo.dev/

