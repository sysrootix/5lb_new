# Комплект для миграции 5LB на новый сервер

## 📦 Что входит в комплект

### Скрипты

1. **migration-backup.sh** - Резервное копирование на старом сервере
   - Создает полный бэкап БД, кода и конфигураций
   - Запускать: `./migration-backup.sh`

2. **migration-restore.sh** - Автоматическое восстановление на новом сервере
   - Устанавливает зависимости и восстанавливает все данные
   - Запускать: `./migration-restore.sh`

3. **migration-verify.sh** - Проверка работоспособности
   - Автоматически проверяет все компоненты системы
   - Запускать: `./migration-verify.sh`

### Документация

1. **MIGRATION-QUICK-START.md** - Краткая инструкция
   - Читать в первую очередь
   - Пошаговый план на одной странице

2. **MIGRATION-GUIDE.md** - Полное руководство
   - Детальное описание всех этапов
   - Раздел Troubleshooting
   - Ручная настройка если нужно

3. **MIGRATION-README.md** - Этот файл
   - Обзор всего комплекта

---

## 🚀 Быстрый старт (3 команды)

### На старом сервере:
```bash
cd /root/5lb
./migration-backup.sh
```

### Копируем данные:
```bash
scp -r /root/5lb-migration-* root@НОВЫЙ_IP:/root/
```

### На новом сервере:
```bash
cd /root/5lb
./migration-restore.sh && ./migration-verify.sh
```

---

## 📊 Процесс миграции (визуально)

```
СТАРЫЙ СЕРВЕР                    НОВЫЙ СЕРВЕР
     │                                │
     │ 1. migration-backup.sh         │
     │    └─> /root/5lb-migration-*/  │
     │                                │
     │ 2. scp → → → → → → → → → → → → │
     │                                │
     │                           3. migration-restore.sh
     │                                │
     │                           4. migration-verify.sh
     │                                │
     │                           5. Переключить DNS
     │                                │
     ✓ Старый                    ✓ Новый (основной)
```

---

## 📋 Что будет перенесено

- ✅ **База данных PostgreSQL** (lb_db)
  - Все таблицы и данные
  - Пользователи и права

- ✅ **Код проекта**
  - Backend (Node.js + Express + Prisma)
  - Frontend (React + Vite)
  - CRM Backend

- ✅ **Конфигурация**
  - .env файлы (DATABASE_URL, API keys, etc.)
  - Nginx конфигурация
  - PM2 процессы

- ✅ **Не переносится** (будет установлено заново)
  - node_modules
  - Системные пакеты

---

## 🎯 Рекомендованный порядок действий

### Подготовка (за день до миграции)

1. **Протестируйте скрипты**
   ```bash
   # На старом сервере
   ./migration-backup.sh
   ```

2. **Оцените размер данных**
   ```bash
   du -sh /root/5lb-migration-*/
   ```

3. **Подготовьте новый сервер**
   - Установите чистую Ubuntu 20.04/22.04
   - Настройте SSH доступ
   - Обновите систему: `apt update && apt upgrade -y`

### День миграции (минимальное время простоя)

#### Вариант 1: С простоем (самый надежный)

```bash
# 1. На старом сервере - остановите приложение
pm2 stop all

# 2. Создайте финальный бэкап
./migration-backup.sh

# 3. Скопируйте на новый сервер
scp -r /root/5lb-migration-* root@НОВЫЙ_IP:/root/

# 4. На новом сервере - восстановите
./migration-restore.sh

# 5. Проверьте
./migration-verify.sh

# 6. Переключите DNS
# (в панели управления доменом)

# 7. После проверки - запустите старый сервер обратно
# (на случай проблем)
pm2 start all
```

**Время простоя:** 15-30 минут

#### Вариант 2: Без простоя (сложнее)

```bash
# 1. Создайте бэкап БЕЗ остановки приложения
./migration-backup.sh

# 2. Настройте новый сервер
./migration-restore.sh

# 3. Синхронизируйте данные за время миграции
# (ручная синхронизация изменений в БД)

# 4. Быстро переключите DNS

# 5. Синхронизируйте последние изменения
```

**Рекомендуется:** Вариант 1 для большинства случаев

---

## 🔍 Проверка после миграции

### Автоматическая проверка
```bash
./migration-verify.sh
```

### Ручная проверка

1. **Сервисы**
   ```bash
   systemctl status postgresql
   systemctl status nginx
   pm2 list
   ```

2. **API**
   ```bash
   curl http://localhost:4000/api/health
   ```

3. **База данных**
   ```bash
   psql -U lb_user -h localhost -d lb_db -c "SELECT COUNT(*) FROM \"User\""
   ```

4. **Функциональность** (в браузере)
   - [ ] Авторизация работает
   - [ ] Каталог товаров загружается
   - [ ] Корзина функционирует
   - [ ] Профиль пользователя открывается
   - [ ] CRM панель работает

---

## 🆘 Если что-то пошло не так

### Откат на старый сервер

1. Не трогайте старый сервер до полной проверки нового
2. Если проблемы - переключите DNS обратно на старый IP
3. Изучите логи на новом сервере:
   ```bash
   pm2 logs
   tail -f /var/log/nginx/error.log
   ```

### Частые проблемы

| Проблема | Решение |
|----------|---------|
| PM2 процессы падают | `pm2 logs` → проверить .env файлы |
| База не подключается | Проверить DATABASE_URL в .env |
| Nginx 502 | Проверить что backend запущен: `pm2 list` |
| API не отвечает | Проверить порт 4000: `netstat -tuln \| grep 4000` |

**Полный список:** см. MIGRATION-GUIDE.md раздел Troubleshooting

---

## 📞 Поддержка

### Полезные команды

```bash
# Логи
pm2 logs 5lb-backend
pm2 logs 5lb-crm-backend
tail -f /var/log/nginx/error.log

# Статус
pm2 list
systemctl status postgresql
systemctl status nginx

# Перезапуск
pm2 restart all
systemctl restart nginx

# База данных
psql -U lb_user -h localhost -d lb_db
```

### Файлы для проверки

- `/root/5lb/backend/.env` - настройки backend
- `/root/5lb/frontend/.env` - настройки frontend
- `/etc/nginx/sites-enabled/` - конфигурация Nginx
- `~/.pm2/logs/` - логи PM2

---

## ✅ Финальный чеклист

```
Перед миграцией:
[ ] Протестирован migration-backup.sh
[ ] Подготовлен новый сервер
[ ] Есть доступ по SSH к обоим серверам
[ ] Известен новый IP адрес

Во время миграции:
[ ] Создан бэкап (migration-backup.sh)
[ ] Бэкап скопирован на новый сервер
[ ] Выполнено восстановление (migration-restore.sh)
[ ] .env файлы проверены и обновлены
[ ] Пройдена проверка (migration-verify.sh)
[ ] Проведено ручное тестирование

После миграции:
[ ] SSL сертификат настроен
[ ] DNS переключен на новый IP
[ ] Автозапуск PM2 настроен
[ ] Файрвол настроен
[ ] Работает стабильно 48+ часов
[ ] Старый сервер можно отключать
```

---

## 📚 Документация

- **Начните здесь:** MIGRATION-QUICK-START.md
- **Полное руководство:** MIGRATION-GUIDE.md
- **Этот файл:** MIGRATION-README.md

---

## 💡 Советы

1. **Делайте миграцию в нерабочее время** (ночь/выходные) для минимизации влияния на пользователей

2. **Не удаляйте старый сервер сразу** - держите его включенным минимум 2-3 дня после миграции

3. **Мониторьте логи** первые дни после миграции:
   ```bash
   pm2 logs --lines 1000
   ```

4. **Сделайте тестовую миграцию** на временном сервере если есть возможность

5. **Документируйте все изменения** которые делаете в процессе

---

## 🎉 Готово!

После успешной миграции у вас будет:
- ✅ Полностью рабочая копия приложения на новом сервере
- ✅ Все данные перенесены
- ✅ Конфигурация идентична старому серверу
- ✅ Приложение доступно по новому IP/домену

**Удачной миграции!** 🚀
